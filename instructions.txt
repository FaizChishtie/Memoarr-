Download git:
https://git-scm.com/download/mac

Instructions:
cd to folder

(ONLY FIRST TIME) git clone https://github.com/FaizChishtie/Memoarr-.git

Adding files:
git add -A

git commit -m “text”

git push

Get files:
cd to folder

git pull